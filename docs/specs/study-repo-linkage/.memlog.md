---
topic: Convention-based linkage and reorganization of the my-studies (notes) and my-studies-code (code) sibling repositories, plus a CLI that generates indexes, validates frontmatter, and checks cross-repo references
updated: 2026-08-21T19:11
---

- (direction) User asked to connect my-studies (notes) and my-studies-code (code) and improve organization across study resources; primary platforms Platzi and TryHackMe, occasional external sources.
- (note) Survey: notes repo has 172 md files (134 leaf notes, 38 index READMEs) across 10 platform folders; code repo has 6 top-level folders. Only 4 overlap (Books, Midu.dev, TypeHero, Udemy).
- (note) Survey: no note in either repo carries frontmatter, so nothing can answer "what am I halfway through".
- (note) Survey: repos already commit in lockstep by hand (C book Ch2/Ch3 and typehero Default Generic Arguments each landed same-day in both). The link exists but is unrecorded.
- (decision) No git submodules. Reciprocal submodules were already tried (my-studies@38ac0af mounted code at "Code Examples", my-studies-code@fe254a1 mounted notes at "Notes") and both were torn out during the Mar-Apr 2026 migration commits. Linkage is convention plus generation only.
- (decision) Repos stay separate rather than merging into a monorepo, because the code repo carries heavy binaries (2K wood textures, Phaser sprite sheets, 3D models) plus node_modules on disk, and Obsidian has no true ignore mechanism, so a merged vault would index all of it.
- (decision) Target editor is Obsidian on the notes repo. User already writes [[wikilinks]] for unwritten notes (TryHackMe/README.md:11, C book README:15-20), so the syntax needs no migration.
- (constraint) Index generation must write into a delimited managed block, never overwrite whole files: 14 of the 38 index READMEs carry hand-written prose (e.g. the "History of C" section) that full-file generation would destroy.
- (constraint) Frontmatter "code" is a list, not a scalar. One note can map to several sibling directories; the Udemy Astro course alone maps to 6 project folders.
- (constraint) Unresolved [[wikilinks]] are intentional backlog markers for planned notes, not broken links. Validation reports them as planned and must never fail on them.
- (constraint) Cross-repo references are relative paths assuming sibling checkout under a common parent (c:\MyStudies). Frontmatter also carries the canonical GitHub URL because relative cross-repo links do not resolve on github.com.
- (constraint) Tool must run on both Windows and WSL2; path handling cannot assume a separator.
- (decision) CLI is Node plus TypeScript, matching the JS/TS/Astro/Vue/Angular content of the code repo, and supplying the package.json the repo currently lacks (it has an orphan 100-byte package-lock.json and no package.json).
- (decision) Tool lives at my-studies-code/tools/studylink, keeping source files out of the Obsidian vault.
- (decision) Migration is one automated pass over the 134 leaf notes: infer source, slug and tags from folder path, derive started/finished from first and last commit touching each file, land it as a single reviewable diff. Notes untouched for 90+ days default to status done.
- (decision) Existing platform-first folder layout is preserved. Topic retrieval comes from tags and maps-of-content, not folder moves, so no note changes path during migration.
- (note) Dead tooling to remove or repair: my-studies/eslint.config.mjs ignores **/* so it is a no-op; .prettierrc.js and .markdownlint.json exist with no package.json to run them; my-studies-code has a package-lock.json with no package.json.
- (note) Observed drift the generator fixes: my-studies/Midu.dev/README.md lists "Lo ultimo de JavaScript" twice (lines 5 and 7).
- (decision) Lumen (github.com/LuigiEspinosa/Lumen) stays archived and out of scope. It holds 1 commit, language null, and the user archived it on 2026-08-16. Building a note app competes with studying for the same hours; the CLI is the small version worth building.
- (assumption) Assumed slug shape platform/course[/note] derived from existing folder names, since folder names already mirror the source course titles.
- (question) Staleness threshold for the status command: is 30 days the right cutoff for flagging an active course as stalled?
- (question) Should Platzi and TryHackMe get code-repo folders created up front, or only when a course or room first produces a script?
- (question) Should the migration also repair or delete the dead lint tooling, or is that a separate cleanup?
- (decision) Inkdrop rejected on a structural fact, not preference: it stores notes in an internal PouchDB database rather than plain files on disk, so it cannot take an existing git repo of markdown as its source of truth. Also now USD 9.98/month with no free tier.
- (decision) Foam in VS Code considered as runner-up (actively maintained, last push 2026-08-13, 17.3k stars) and remains compatible, since both tools operate on plain files. Obsidian chosen as primary reading and linking surface; the spec depends on neither beyond plain-markdown compatibility.
- (note) Corpus measured: 172 md files total, 134 leaf notes, 38 index READMEs, of which 14 carry hand-written prose beyond their link lists.
- (decision) Date derivation excludes the March 2026 "chore(migration): ... Lint" bulk commits, which touched whole platform folders at once and would otherwise flatten finished dates across every note in a platform.
- (event) Self-validate pass 1 (coherence, Spec Law 1-6 and 8): PASS. All 6 capabilities carry intent and success; intents stay at WHAT with implementation detail routed to cli-contract.md; all 9 constraints rule something out; 6 non-goals present; success signal is demonstrable via a command exit and a vault query.
- (event) Self-validate pass 2 (preservation, Spec Law 7): PASS after one repair. All load-bearing claims from the user request and the repo survey land in SPEC.md or a companion. Gap found and fixed: the Inkdrop and Foam evaluation rationale was absent from the memlog and has been appended. Wrapper-only content dropped: BMad routing discussion and conversational framing.
- (decision) Staleness cutoff confirmed at 30 days for flagging an active resource as stalled.
- (decision) Platzi and TryHackMe code-repo folders are created only when a course or room first produces code, not up front. Resolves the open question; empty scaffolding is not created.
- (decision) Dead lint tooling repair is in scope for this work rather than a separate cleanup. Adds CAP-7.
- (direction) User challenged the done-versus-stalled distinction, which exposed a flaw in the migration rule.
- (decision) SUPERSEDES the earlier 90-day rule: completion is never inferred from dates. Dates can only ever show that nothing is happening, never whether that is because the course finished or was abandoned. The two are indistinguishable in timestamp data.
- (decision) Completion requires a coverage signal: outline_total (units the source actually has) versus units written. status done is human-authored and validated against coverage; stalled is derived (status active plus no touch in 30 days) and is never a stored status value; dropped stays human-authored intent.
- (note) Coverage data measured across the vault: only 2 of 172 files carry wikilink outlines (Books/The C Programming Language README with 5 unresolved, TryHackMe/README with 3), 8 unresolved targets total, 0 resolved. So the outline signal is real but exists for 2 of roughly 38 resources today.
- (decision) Because coverage is unknown for 36 of 38 resources, migration assigns status active to everything not provably complete and emits a one-time triage list of roughly 38 resources for the human to settle. It never fabricates done. This replaces auto-marking 108 notes as done.
- (decision) studylink status gains a --triage mode that walks stalled resources and writes the chosen status back, so ongoing upkeep does not depend on the author remembering to edit frontmatter.
- (constraint) studylink validate flags the contradiction where status is done but coverage is below 100 percent, so a wrong call surfaces instead of sitting silently.
- (decision) Status confirmed done: Books/ASP.Net Core 3 and React; all 5 Midu.dev workshops; Santander High-Performance Leadership; all 12 Veeva Learning certifications; TryHackMe Advent of Cyber 2024 (author confirms done despite Day 24 being absent, so coverage is not treated as authoritative over the human call).
- (note) Author requested dropping: all Udemy, Books/The C Programming Language 2nd Ed, all TryHackMe except Advent of Cyber, TypeHero, roadmap.sh, Coding Interview University, all Platzi. Stated reasons vary: Udemy stalled and outdated, TryHackMe stalled and wanting updated courses, Platzi mostly done but outdated.
- (note) Drop set measured before acting: 51 of 172 notes, 253,348 bytes, leaving 121 notes. Largest at risk are Platzi Curso de Finanzas para Startups (23.0 KB), TryHackMe Active Directory Basics (22.1 KB), Platzi Curso Avanzado de Gramatica en Ingles (14.7 KB), TryHackMe Linux Fundamentals Part 3 (13.9 KB).
- (note) Dropping notes would orphan sibling code with no note counterpart: Udemy 330 files (6 Astro projects, 3 Phaser games), Books C chapters 1-3 exercises, TypeHero 8 files. This works against CAP-2, so the code side needs an explicit decision too.
- (question) Does "drop" mean status dropped in frontmatter, archive the files, or delete them from the working tree? Blocking on the author before any destructive action.
- (decision) RESOLVES the drop question: author chose delete from the working tree for the notes, and drop the matching code too, with the 253 KB and 350-file impact stated in front of them. Concern was raised and the choice reaffirmed, so it was executed in full.
- (event) Executed on 2026-08-21. my-studies commit 5e13fde "Drop stalled and outdated course notes", 60 files changed, 4733 deletions. my-studies-code commit 0797c5d "Drop code for dropped courses", 402 files, 70832 deletions. Both local only, not pushed. Both trees clean.
- (note) Index READMEs repaired by hand as part of the drop, since the generator does not exist yet: my-studies/README.md (10 platform links down to 5), Books/README.md (C book link removed), TryHackMe/README.md (rewritten to Advent of Cyber only).
- (note) Corpus after the drop: 121 notes (99 leaf, 22 index), 679,769 bytes, 21 resources across 5 platforms. Was 172 notes across 10 platforms.
- (decision) Every one of the 21 surviving resources is status done: Books/ASP.Net Core 3 and React, 5 Midu.dev workshops, Santander High-Performance Leadership, TryHackMe Advent of Cyber 2024, and 13 Veeva Learning certifications. There are no active, backlog, or stalled resources at migration time.
- (decision) SUPERSEDES the triage design: migration no longer needs a human triage pass, because the author settled every status directly and all 21 resolve to done. Migration writes status done uniformly and derives dates from git.
- (note) Zero unresolved wikilinks remain; both files that carried them (Books/The C Programming Language README and TryHackMe/README) were in the drop set. The planned-notes constraint stays in the contract because outlines will be written again, but it has no current instances.
- (decision) Coverage is advisory, never a gate. Author marked Advent of Cyber 2024 done despite Day 24 being absent, establishing that the human status call overrides the coverage signal; validate warns on the contradiction rather than failing.
- (note) CAP-4 status reporting has nothing to report on day one, since no resource is active. It exists for the study that follows, not for the current corpus.
- (event) Self-validate pass 1 (coherence, Spec Law 1-6 and 8) on the re-derived spec: PASS. 7 capabilities each carry intent and success; CAP-1 to CAP-6 IDs preserved from the prior derive and CAP-7 took the next unused ID; 11 constraints each rule something out; 7 non-goals; success signal is demonstrable via a command exit plus a vault query.
- (event) Self-validate pass 2 (preservation, Spec Law 7) on the re-derived spec: PASS after repairs. Every load-bearing claim from this session lands in SPEC.md or a companion. Repairs made: every worked example in frontmatter-schema.md cited a course deleted in the prune (Udemy Astro, C book, TryHackMe Search Skills) and was replaced with surviving resources; the code-is-a-list justification moved from the Udemy Astro 6-project case to the surviving Midu.dev Experiencias 3D three-lesson case; the managed-block example moved from the C book to Midu.dev; prose-bearing README count corrected from 14 to 11.
- (note) Documented a real join case discovered during repair: the notes side spells the folder "ASP.Net Core 3 and React" and the code side "ASP.NET Core 3 and React". The shared slug is what joins them, which is the concrete argument for identity living in the slug rather than the path.
- (note) Out of scope but flagged to the author: my-studies-code working tree is 466 MB, of which 462 MB is 83 binary assets all under Midu.dev, including standing-desk.glb triplicated at roughly 60 MB per lesson variant. Around 184 MB is duplicate glb content bloating every clone.
- (event) Remote housekeeping 2026-08-21: both repos pushed to origin/main (my-studies 5e13fde, my-studies-code 0797c5d). Deleted remote branches codewars, exercism-js, leetcode from my-studies-code. Both repos are now single-branch, local and remote.
- (decision) Author chose to delete origin/codewars and lose its 2 katas (6 files present nowhere on main), informed that they existed only on that branch. SHAs recorded before deletion for the GitHub recovery window: codewars 7c6e8483, exercism-js fbaa93c8, leetcode ef7109b9.
- (decision) Author then dropped the Exercism and LeetCode directories from main as well. Commit 837a7da "Drop Exercism and LeetCode", 29 tracked files, pushed.
- (decision) SUPERSEDES the code-only asymmetry handling: my-studies-code now contains only Books and Midu.dev, both of which have note counterparts. There are zero code directories without notes, so validate needs no suppression list and the Exercism/LeetCode carve-out is removed from the contract. The general rule that a code directory may lack notes stays, with no current instances.
- (decision) Spec is committed into my-studies-code as the durable record. _bmad-output remains the working location where bmad-spec re-derives, so the repo copy is a published snapshot that must be refreshed after any re-derive.
- (direction) User requested Story Breakdown against the finalized spec, walked conversationally, with spec_checkpoint, done_checkpoint and invoke_dev_with elicited per story rather than defaulted.
- (note) Corpus re-measured during breakdown: the 22 index READMEs sit in 3 tiers, 1 root plus 5 platform plus 16 resource. 17 of 22 carry ## sections, 10 open with an anchor-link table of contents into their own sections, and 11 link notes belonging to other resources.
- (note) Index link text is not mechanically derivable. Three rules across three files: the ASP.NET Chapter 6 label equals its H1, the Advent of Cyber Day 1 label is filename stem plus H1, and the Day 11 label ends with an exclamation mark its H1 lacks. No derivation can invent that character.
- (decision) SUPERSEDES folder-derived index generation and the order-by-slug rule: managed blocks are seeded once per existing section from what is on disk, capturing authored membership and link text. The generator never invents membership or labels; inside a seeded block it dedupes, drops dead links, and inserts notes that exist on disk but appear in no block. This is what makes byte-identical regeneration achievable across all 22 indexes, including the 11 carrying cross-resource links.
- (note) Contract gap found: validation rule 5 requires 1 to 2 slashes in a slug, so the root README and the 5 platform READMEs cannot hold a valid slug, and the root README has no source value either. Migration writes frontmatter to all 121 files, so validate would fail on 6 and CAP-1 exit-0-across-121 was unreachable as written.
- (decision) RESOLVES the slug gap: kind gains a third value platform, carrying a reduced required-field set with no slug, started or finished. It covers the root README and the 5 platform indexes. The slug regex stays unchanged and all 121 files stay inside the contract.
- (note) Correction: cli-contract.md says validate checks 10 rules; frontmatter-schema.md defines 11. The schema is authoritative.
- (note) Correction: the frontmatter-schema index worked example gives the ASP.NET H1 as ASP.NET Core 3 and React, but on disk it is the full title ending by Carl Rippon, which is exactly the link text Books/README.md carries. The example understated the case that proves labels are stored, not derived.
- (decision) Story slate is 6, splitting the migrate command from the migration run. Reviewing migrate.ts is a code review; reviewing the resulting 121-file diff against the verification checklist is a content review with a different failure mode and a different rollback.
- (decision) Ordering inside a seeded managed block is frozen at seed time: dedup keeps the first occurrence, dead links drop in place, and a newly discovered note appends at the end of its block. Replaces order-by-slug, which would have reordered the 11 Veeva teaching-order blocks and sorted Advent of Cyber to Day 1, Day 10, Day 11, Day 2.
- (decision) kind platform covers the root README as well as the 5 platform indexes. Both need the same reduced field set, so no separate root kind is introduced.
- (decision) Story 1, tool skeleton plus lint wiring (CAP-7). package.json in both repos, repo discovery, schema module carrying the three-value kind enum, working markdownlint and prettier including the managed-block ignore. spec_checkpoint true, done_checkpoint true.
- (decision) Story 2, studylink validate (CAP-1, CAP-2). All 11 rules, the three warnings that do not affect exit code, cross-repo path resolution from the note containing directory. Fixture-driven, because no note carries frontmatter until story 6. spec_checkpoint true, done_checkpoint false.
- (decision) Story 3, studylink index (CAP-3). Seed a managed block per authored section, maintain in place, idempotent second run, reverse-link blocks into the 5 mapped code directories. Byte-identical acceptance proven here against fixture copies of the three hand-repaired indexes. spec_checkpoint true, done_checkpoint false.
- (decision) Story 4, studylink status (CAP-4). Last-touch dates via git.ts, derived staleness at a 30-day default, coverage reporting, --triage write-back. Entirely fixture-driven since no resource is active; the live corpus only exercises the quiet report. spec_checkpoint true, done_checkpoint false.
- (decision) Story 5, studylink migrate as a command (CAP-5). Git-derived dates excluding the Lint reformatting commits and the two prune commits, uniform status done, empty tags, code populated for 5 directories only. Dry run against a scratch vault copy, never the real one. spec_checkpoint true, done_checkpoint true.
- (decision) Story 6, run the migration and open the vault (CAP-5, CAP-6). The single reviewable diff, validate exits 0, index written twice for an empty second diff, the three hand-repaired indexes byte-identical, all nine verification-checklist boxes, then Obsidian. The only story that writes to both real repos. spec_checkpoint true, done_checkpoint true.
- (event) Re-derived after the breakdown decisions. SPEC.md: CAP-3 intent restated from frontmatter-derived link lists to managed blocks kept consistent with disk; 3 constraints added covering seeded membership, frozen block order, and the platform tier. Constraint count 11 to 14. All 7 capability IDs preserved. frontmatter-schema.md: kind gains platform with a Kinds table and reduced field set, rules 1, 2 and 11 amended, rules 3 to 6 exempted for platform, ASP.NET worked-example H1 corrected to the full title, platform worked example added, and an explicit note that index labels are not frontmatter. cli-contract.md: 10 rules corrected to 11, the managed-block section replaced with the seed-and-maintain design plus the index-shape and label-derivation tables, order-by-slug removed, reverse-link directory count added.
- (event) stories.yaml written with 6 stories and checked against all 4 validity rules in assets/stories-schema.md: PASS. Parses as a top-level list, ids 1 to 6 unique and quoted strings of digits only, prefix-free, no status field on any entry, both checkpoint fields boolean throughout. spec_checkpoint true on all 6; done_checkpoint true on 1, 5 and 6.
- (note) The committed snapshot at my-studies-code/docs/specs/study-repo-linkage/ is now stale in SPEC.md, frontmatter-schema.md and cli-contract.md, and lacks stories.yaml. bmad-spec only writes to _bmad-output, so the snapshot needs a manual refresh.
- (note) Survey during the snapshot refresh: my-studies-code carries no lint configuration at all, only the orphan package-lock.json. cli-contract.md claimed the new scripts drive the .markdownlint.json and .prettierrc.js already present, which is true of my-studies and false of my-studies-code. Story 1 must author both config files there rather than only wiring scripts.
- (note) my-studies/.prettierrc.js uses module.exports, so copying it into my-studies-code breaks if that package.json declares type module for the TypeScript tool. A .prettierrc.json in the code repo sidesteps the conflict.
- (note) Line endings measured: core.autocrlf is true and neither repo has a .gitattributes, so the working tree is uniformly CRLF on Windows while the object database is mixed. my-studies stores 22 of 158 files with CRLF, 19 of them Veeva markdown notes; my-studies-code stores 264 of 376, only 8 of them markdown. endOfLine crlf in .prettierrc.js therefore passes on Windows by accident of the autocrlf setting and would fail on every file under WSL2, so CAP-7 could not hold in both environments as specified.
- (decision) RESOLVES the line-ending conflict: both repos standardize on LF. prettier endOfLine becomes lf, each repo gains a .gitattributes carrying the rule text=auto eol=lf for all paths, and git add --renormalize lands the one-time rewrite of 22 stored files in notes and 264 in code. This makes the Windows and WSL2 constraint genuinely true rather than accidentally true, and removes the mixed-endings drift. The renormalization belongs to story 1 so it stays clear of the single reviewable migration diff in story 6.
- (event) Re-derived after the CAP-7 findings. brownfield.md gains an explicit dead-tooling entry that my-studies-code carries no lint config, the CommonJS prettier caveat, and a Line endings section with the measured CRLF counts. cli-contract.md corrects the claim that both repos already carry lint config and adds the LF settlement with the gitattributes rule and the renormalize step. stories.yaml story 1 dispatch note extended to cover authoring the code-repo lint config and the renormalization; re-checked against all 4 validity rules: PASS. SPEC.md needed no change, since the Windows and WSL2 constraint it already carries is exactly what the finding tested against.
- (event) Snapshot at my-studies-code/docs/specs/study-repo-linkage refreshed to match _bmad-output: SPEC.md, frontmatter-schema.md, cli-contract.md, brownfield.md and .memlog.md updated, stories.yaml added. The snapshot README gains a stories.yaml entry recording that the file is archived there while deliberately staying out of companions:.
